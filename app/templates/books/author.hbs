{{outlet}}
<h2 id="title">Welcome to Ember</h2>

<div id="dropzone">
    <form action="http://localhost:3000/upload" class="dropzone needsclick dz-clickable" id="demo">
        <div class="dz-message needsclick">
            Drop files here or click to upload.<br>
            <span class="note needsclick">(This is just a demo dropzone. Selected files are <strong>not</strong> actually uploaded.)</span>
        </div>
    </form>
</div>
{{outlet}}

{{#x-file-input class="custom-class" action="uploadAPhoto"}}
    <h3>Shall you upload?</h3>
{{/x-file-input}}

<br/>
new..with actions<br/>
{{#x-file-input multiple=false action=(action "didSelectFiles")}}
    <h3>Shall you upload?</h3>
{{/x-file-input}}

<script type="text/javascript">
    //Dropzone.autoDiscover = false;
    //var myDropzone = new Dropzone(element, {
    //    url: "http://localhost:3000/upload.php",
    //    autoProcessQueue: false
    //});
    //
    //$('#imgsubbutt').click(function ()
    //                       {
    //                           myDropzone.processQueue();
    //                       });

    Dropzone.options.demo = {
        init: function ()
        {
            this.on("addedfile", function (file)
            {
                alert("Added file.");
            });

            this.on("success", function (file)
            {
                alert("Sucessfully uploaded file.");
            });
        },

        dictDefaultMessage: "Put your custom message here",
        acceptedFiles: 'application/vnd.ms-excel,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
        uploadMultiple: 'false',
        url: 'http://192.168.1.198:3000/upload',
        accept: function (file, done)
        {
            if (file.name == "justinbieber.jpg")
            {
                done("Naha, you don't.");
            }
            else
            {
                done();
            }
        }
    };
</script>


<!--"C:\Program Files (x86)\Google\Chrome\Application\chrome.exe" --user-data-dir="C:/Chrome dev session" --disable-web-security-->